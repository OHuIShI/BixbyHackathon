action (GetQuiz) {
  description (영어단어퀴즈)
  type(Constructor)
  collect {
    input (num) {
      type (Num)
      min (Required) max (One)
      default-init{
        intent {
          goal : SetNum
        }
      }
    }
  }
  output (ProgressInfo)
}
