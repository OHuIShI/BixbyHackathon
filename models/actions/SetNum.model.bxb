action (SetNum) {
  description (num 입력받기)
  type(Constructor)
  collect {
    input (num) {
      type (Num)
      min (Required) max (One)
      default-init {
        intent {
          goal : GetNum
        }
      }
    }
  }
  output (Num)
}
